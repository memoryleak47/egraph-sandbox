use crate::*;

// maps between the AST & RecExpr using De Bruijn versions.

pub fn ast_to_re(ast: &Ast) -> RecExpr<ENode> {
    todo!()
}

pub fn re_to_ast(re: &RecExpr<ENode>) -> Ast {
    todo!()
}
